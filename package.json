{
  "name": "macro-lens",
  "version": "1.0.0",
  "private": true,
  "description": "Static FRED macro dashboard for GitHub Pages and Actions",
  "scripts": {
    "fetch-data": "node scripts/fetch-fred-data.js"
  },
  "engines": {
    "node": ">=24"
  }
}
